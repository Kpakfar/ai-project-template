Quick reference. {{LANGUAGE}} project bootstrapped from ai-project-template.
Update this file whenever the layout changes.

Living docs:
docs/structure.txt                 # this file
docs/requirements.md               # what we're building, for whom, why
docs/language-standards.md         # language- and tooling-specific conventions
docs/documentation.md              # links to library docs the agent should consult
docs/backlog.md                    # scoped, queued tasks
docs/proposals-ideas.md            # out-of-scope ideas, reviewed periodically
docs/gotchas.md                    # known pitfalls, anti-patterns, lessons (living)
docs/current-task/task.md          # active task brief, shared between agents
docs/current-task/task-template.md # template; copy over task.md when starting a new task

Source code (two-layer split, see AGENTS.md <architecture-discipline>):
src/backend/                       # domain logic and I/O. One concept per file.
src/frontend/                      # UI layer. Or top-level app.{{LANG_EXT}}.

Tests:
tests/                             # mirrors src/ structure; layout per docs/language-standards.md

Configuration:
{{MANIFEST_FILE}}                  # dependencies + tool config + bundled scripts
.env.example                       # template for environment variables (never commit .env)
.gitignore
.pre-commit-config.yaml            # local pre-commit hooks (optional)
README.md

Agent infrastructure:
.claude/agents/                    # subagent definitions (test-spec-writer, implementer, code-reviewer)
.claude/hooks/quality-gate.sh      # deterministic QA hook triggered by code-reviewer Stop event
.mcp.json                          # MCP server config (Context7 for live library docs)
AGENTS.md                          # the constitution (read first, every conversation)
CLAUDE.md                          # symlink to AGENTS.md (for Claude Code compatibility)

CI:
.github/workflows/qa.yml           # runs the quality-gate command on every push and pull request
.github/pull_request_template.md   # short PR checklist

Dev environment:
.devcontainer/                     # dev container config (if {{USES_DEVCONTAINER}} = yes)
scripts/                           # project scripts (qa runner and any others added via [project.scripts])
